# damana-v2 — C4 Container Diagram (L2)

> **Note**: this diagram was auto-generated by /handover on 2026-05-27 from repo signals (docker-compose.yml, requirements.txt, go.mod, .env.example, README.md). It is a **starting point** — review and refine.
>
> - Container labels and tech strings — the detector inferred these from compose service images and package deps; verify against the actual deployment
> - Inferred relationships — data flows are approximate; gRPC and REST boundaries in particular should be confirmed
> - External systems — anything your team uses that isn't in package.json / go.mod (e.g. infra-only dependencies, direct cloud APIs) won't have been detected
>
> Update the "Maintenance" section below once the diagram is stable.

## Maintenance

(From the template — update when L2 containers change.)

---

```mermaid
C4Container
    title damana-v2 — Backup-as-a-Service Platform (L2)

    Person(tenant_admin, "Tenant Admin", "Manages backups, devices, policies")
    Person(platform_admin, "Platform Admin", "Manages tenants and platform config")

    System_Boundary(damana, "Damana v2") {
        Container(web, "Web App", "Python / Django 6 + Gunicorn", "Multi-tenant dashboard, platform admin, REST APIs, Celery task dispatch")
        Container(celery, "Background Worker", "Python / Celery 5 + Celery Beat", "Backup runs, retention sweeps, alert lifecycle, offboard tasks, orphan reaping")
        Container(grpc_server, "gRPC Server", "Go 1.25 / gRPC", "Agent command dispatch, CRL publishing, storage credential brokering, stream commands")
        Container(agent, "Backup Agent", "Go 1.25 (distributed binary)", "Installed on backup sources — executes jobs, streams data to storage, reports status via gRPC")

        ContainerDb(db, "Primary Database", "TimescaleDB / PostgreSQL 16", "Tenants, devices, catalog, policies, runs, audit log, legal holds")
        ContainerDb(cache, "Cache + Broker", "Redis 7", "Celery task queue, rate limiting, CRL store (damana:crl:current), session data")
        ContainerDb(storage, "Object Storage", "MinIO (S3-compatible)", "Backup data, agent binaries, restore payloads")
        ContainerDb(logstore, "Log Store", "OpenSearch 2.19.2", "Structured application logs, audit events, OTel log pipeline")
    }

    System_Ext(otel_collector, "OTel Collector", "Receives traces, metrics, logs via OTLP; fans out to backends")
    System_Ext(prometheus, "Prometheus", "Metrics scraping + alerting rules")
    System_Ext(grafana, "Grafana", "Metrics dashboards and oncall visibility")

    Rel(tenant_admin, web, "HTTPS / browser")
    Rel(platform_admin, web, "HTTPS / browser")
    Rel(web, db, "SQL (psycopg3)")
    Rel(web, cache, "TCP / redis-py")
    Rel(web, storage, "S3 API (boto3)")
    Rel(web, grpc_server, "gRPC (internal loopback)")
    Rel(celery, db, "SQL (psycopg3)")
    Rel(celery, cache, "TCP (Celery broker)")
    Rel(celery, storage, "S3 API (boto3)")
    Rel(celery, grpc_server, "gRPC (offboard commands)")
    Rel(grpc_server, db, "SQL (pgx/v5)")
    Rel(grpc_server, cache, "TCP (go-redis v9)")
    Rel(grpc_server, storage, "S3 API (aws-sdk-go-v2)")
    Rel(agent, grpc_server, "mTLS gRPC (50051)")
    Rel(agent, storage, "S3 API (direct, credentialed by gRPC server)")
    Rel(web, otel_collector, "OTLP HTTP (traces + metrics + logs)")
    Rel(celery, otel_collector, "OTLP HTTP")
    Rel(grpc_server, otel_collector, "OTLP gRPC")
    Rel(otel_collector, logstore, "OpenSearch bulk ingest")
    Rel(otel_collector, prometheus, "Prometheus remote-write / scrape")
    Rel(prometheus, grafana, "PromQL")
```
